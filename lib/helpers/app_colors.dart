import 'package:flutter/material.dart';

class AppColors {
  // ── SoloTrek Brand Colors ──────────────────────────────────────────
  static const background = Color(0xFFF5F0E8); // cream
  static const card = Colors.white;
  static const primary = Color(0xFF1A3557); // navy
  static const primaryLight = Color(0xFFEAF0F8); // navy muda
  static const accent = Color(0xFF2ABFBF); // teal
  static const accentLight = Color(0xFFE6F9F9); // teal muda

  // ── Status Colors (tetap informatif tapi harmonis) ─────────────────
  static const success = Color(0xFF16A34A);
  static const successLight = Color(0xFFF0FDF4);
  static const warning = Color(0xFFD97706);
  static const warningLight = Color(0xFFFFFBEB);
  static const danger = Color(0xFFDC2626);
  static const dangerLight = Color(0xFFFEF2F2);

  // ── Text & Border ──────────────────────────────────────────────────
  static const textPrimary = Color(0xFF1A3557); // navy
  static const textSecondary = Color(0xFF6B7280);
  static const border = Color(0xFFE5E7EB);

  // ── Misc ───────────────────────────────────────────────────────────
  static const qiblaGold = Color(0xFFF59E0B);
  static const qiblaGoldLight = Color(0xFFFFFBEB);
}
